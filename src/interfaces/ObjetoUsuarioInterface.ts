/**
 * Interfaz que define las propiedades de un objecto de tipo Usuario
 */
export default interface ObjetoUsuarioInterface{
    codUsuario: string
    password: string,
    descripcion: string,
    tipo: string
}